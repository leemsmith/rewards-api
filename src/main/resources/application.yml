spring:
  application:
    name: rewards-api

  datasource:
    url: jdbc:h2:mem:rewardsdb
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  port: 8080